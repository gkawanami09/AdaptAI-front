# Copy this file to .env.local (gitignored) to enable it locally.

# Set true only when you want to bypass auth locally.
VITE_MOCK_AUTH=false

# FastAPI base URL used by the real auth endpoints.
VITE_API_URL=http://localhost:8000
