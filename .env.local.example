# Copy this file to .env.local (gitignored) to enable it locally.

# Skips the real /usuarios/me call and unlocks protected routes with a
# fake profile, while the backend isn't ready yet.
VITE_MOCK_AUTH=true
